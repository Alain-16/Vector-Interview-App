# Core Django Framework
django==5.2.5

# Django REST Framework for building our API
djangorestframework==3.14.0

# For JWT (JSON Web Token) authentication
djangorestframework-simplejwt==5.3.0

# PostgreSQL database adapter
psycopg2-binary==2.9.7

# Asynchronous Task Queue (for video processing, AI analysis, etc.)
celery==5.3.4
redis==5.0.1  # Celery message broker

# For handling environment variables from .env file
python-dotenv==1.0.0

# AWS SDK for Python (for S3 video storage)
boto3==1.28.57

# For handling Cross-Origin Resource Sharing (CORS)
# Allows our frontend to communicate with the backend API
django-cors-headers==4.2.0

# Production-ready WSGI HTTP Server
gunicorn==21.2.0
